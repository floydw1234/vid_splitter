# Week Board: Mon Aug 31 – Fri Sep 4, 2026

1. **Mon**: Add CI workflow for Python tests on PR/push
   - Why: Only `release.yml` exists; tests don't run automatically on PRs

2. **Tue**: Move `tools/test_bvf_player.py` → `tests/test_bvf_player.py`
   - Why: Consistent test layout; all other tests live in `tests/`

3. **Wed**: Add `--version` flag to `bvf_player.py` and `bvf_probe.py`
   - Why: CLI hygiene; helps users verify installed version

4. **Thu**: Add smoke test for `analyzer/skin_detector.py`
   - Why: No test coverage for skin_detector; basic import/call smoke test

5. **Fri**: Update README tests section + add CI status badge
   - Why: Clarify which tests exist, where they live, and show CI health
