# Ticket 4c044d34-ee0a-42c7-8370-29cfb657c615 Task Plan

## Goal
Add a single comment-only smoke marker near the header of `analyzer/skin_detector.py` while keeping runtime behavior unchanged.

## Scope
- Primary file to change: `analyzer/skin_detector.py`
- Test file to update or add: `tests/test_analyzer_package.py`
- No integration test data, Docker Compose, or UI/E2E work is required for this ticket because the change is source-comment only.

## TDD Execution Order

### 1. Establish the current baseline
1. Inspect `analyzer/skin_detector.py` to confirm the module header layout:
   - module docstring at the top
   - imports immediately after
   - no executable code before the imports
2. Review `tests/test_analyzer_package.py` to see how the analyzer package is already validated.
3. Identify the exact marker text that should be asserted in the test so the change stays minimal and isolated.

### 2. Add a failing test first
1. Update `tests/test_analyzer_package.py` with a focused assertion for `analyzer/skin_detector.py`.
2. The test should verify:
   - the file still starts with a module docstring
   - the new smoke marker comment exists near the file header
   - the marker is a comment line, not a string literal or executable statement
   - the marker does not affect `analyzer.__doc__` or other runtime behavior
3. If useful, assert the relative position of the marker using `splitlines()`, for example checking that the comment appears directly after the module docstring and before imports.
4. Keep the test narrowly scoped to the single file so it fails for the missing marker and nothing else.

### 3. Run the targeted test to confirm the failure
1. Run the smallest relevant test command, centered on `tests/test_analyzer_package.py`.
2. Confirm the test fails for the expected reason: the marker is not yet present in `analyzer/skin_detector.py`.
3. If the failure is not specific enough, tighten the assertion instead of broadening the scope.

### 4. Implement the minimum code change
1. Edit `analyzer/skin_detector.py` only.
2. Insert one comment-only smoke marker near the file header, directly below the module docstring and before imports.
3. Preserve all existing imports, class definitions, and function logic.
4. Do not add helper functions, refactor the detector, or move code unless required to keep the file valid.

### 5. Verify the fix
1. Re-run `tests/test_analyzer_package.py`.
2. Run the broader analyzer-related unit slice if needed to ensure the comment change did not disturb imports or module loading.
3. Confirm that existing behavior remains unchanged and that the new marker is present exactly where the test expects it.

### 6. Minimal cleanup
1. If the new assertion is too brittle or too loose, adjust only the test wording/position checks.
2. Keep the final diff limited to the target module comment and the single verification test.
3. Avoid any follow-on refactors because they are out of scope for a smoke-marker ticket.

## Dependencies
- Step 2 depends on step 1 so the test encodes the intended marker location and style.
- Step 4 depends on step 3 so the implementation is driven by a failing test.
- Step 5 depends on step 4 so the change is verified before closing the ticket.

## Non-Goals
- No integration test fixtures, seed data, test DB setup, or generated sample media are required.
- No Docker Compose orchestration is needed.
- No frontend, Playwright, Cypress, or other UI automation work is needed.
- No behavioral tuning of the skin detector is part of this ticket.

