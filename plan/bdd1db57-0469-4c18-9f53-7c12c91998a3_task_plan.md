## Ticket

`bdd1db57-0469-4c18-9f53-7c12c91998a3` — Smoke 03: clarify `BVF_SPEC.md` note

## Goal

Make one tiny documentation-only change in `BVF_SPEC.md` that identifies the file as concurrency smoke ticket 03, without changing behavior, requirements, or any runtime-facing semantics.

## Constraints

- Keep the change limited to `BVF_SPEC.md`.
- Do not change normative spec language such as `must`, `should`, supported actions, file layout, offsets, or examples in a way that alters meaning.
- Prefer a harmless Markdown/HTML comment or a clearly non-normative note near the document header.
- Avoid touching implementation code unless a test harness absolutely requires it.

## Repo Context Relevant To This Ticket

- Canonical spec file: `BVF_SPEC.md`
- Spec is referenced from:
  - `PROJECT_SPEC.md`
  - `csharp_plugin/README.md`
- No frontend surface is involved.
- No `docker-compose.yml` or related compose setup is present.
- Existing automated tests focus on Python/C# BVF behavior, not Markdown/documentation rendering.

## TDD Execution Plan

### 1. Confirm scope and choose the least risky annotation style

Order:
1. Re-read the top section of `BVF_SPEC.md`.
2. Decide whether to use:
   - a hidden HTML comment like `<!-- concurrency smoke ticket 03 -->`, or
   - a short visible non-normative note under the title block.
3. Prefer the hidden comment unless the ticket explicitly requires visible rendered text.

Files to inspect:
- `BVF_SPEC.md`
- `PROJECT_SPEC.md`

Why first:
- This determines the test shape and keeps the implementation from drifting into normative content.

### 2. Write a failing documentation-focused test first

Order:
1. Add or update a lightweight Python test that reads `BVF_SPEC.md` as text.
2. Make the test fail by asserting the expected smoke-ticket marker is present.
3. Keep the assertion narrow so it verifies traceability without over-constraining wording.

Preferred test location:
- `tests/test_bvf_spec_doc.py` as a new focused test file

Alternative if keeping file count minimal:
- append one test to an existing lightweight test module, but a dedicated doc test file is cleaner for this ticket.

Planned test coverage:
- Assert `BVF_SPEC.md` contains a smoke marker for ticket 03.
- Assert the marker is near the header area rather than embedded in tables or JSON examples.

Example assertion shape:
- read the file text
- inspect only the first 10-20 lines
- assert presence of a token such as `smoke ticket 03` or `concurrency smoke ticket 03`

Why this test:
- It creates an explicit guard that the requested traceability note exists.
- It avoids introducing behavioral tests for a non-behavioral change.

### 3. Implement the minimum change to pass the new test

Order:
1. Edit `BVF_SPEC.md`.
2. Insert the smallest harmless annotation near the top of the file.
3. Do not alter surrounding spec prose except for spacing if needed.

Primary file to touch:
- `BVF_SPEC.md`

Expected implementation shape:
- Best fit: add a one-line hidden HTML comment immediately under the title or version block.
- Fallback: add a one-line visible note explicitly labeled non-normative.

Non-goals during implementation:
- No updates to `PROJECT_SPEC.md`, code, tests outside the targeted doc test, or examples unless the test location choice requires it.

### 4. Run the targeted test and verify it passes

Order:
1. Run the new doc test only.
2. Confirm the previously failing assertion now passes.

Planned command:
- `pytest tests/test_bvf_spec_doc.py`

Why targeted first:
- Fast feedback for the exact requirement before broader regression checks.

### 5. Refactor only if the test or note wording is unnecessarily brittle

Order:
1. Review the test wording and marker wording.
2. If the assertion is too exact, loosen it to check intent rather than a full string match.
3. If the note text is longer than necessary, reduce it to the minimum traceability statement.

Files that may be adjusted:
- `tests/test_bvf_spec_doc.py`
- `BVF_SPEC.md`

Refactor goal:
- Keep both the note and the test durable across harmless formatting edits.

### 6. Run a small regression pass appropriate for a doc-only ticket

Order:
1. Re-run the focused doc test.
2. Optionally run a small existing Python smoke subset if there is a convenient no-heavy-dependency doc-safe suite.
3. Skip heavyweight media/integration suites unless the repo’s normal pre-merge expectations require them.

Planned verification:
- `pytest tests/test_bvf_spec_doc.py`

Optional secondary verification:
- `python -m pytest tests/test_bvf_probe.py -q` only if a broader smoke signal is needed and dependencies are available

Rationale:
- This ticket does not affect code paths, media parsing, networking, or service startup.

## Files Planned To Touch

- New: `plan/bdd1db57-0469-4c18-9f53-7c12c91998a3_task_plan.md`
- Planned later for implementation:
  - `BVF_SPEC.md`
  - `tests/test_bvf_spec_doc.py` or one existing lightweight Python test file if a new file is unnecessary

## Dependencies Between Steps

1. Scope review must happen before test design, because the note may be hidden or visible.
2. The failing test must be added before editing `BVF_SPEC.md`.
3. The doc change should be implemented only after the test is in place and failing.
4. Refactoring should happen only after the targeted test passes.
5. Regression verification comes last.

## Integration / Service / UI Test Considerations

- No app web service changes are involved.
- No localhost ports are needed.
- No Docker Compose setup exists in this repo, so no compose-based integration plan applies here.
- No frontend/UI/E2E tests are relevant to this ticket.
- No external test data is needed; the only artifact under test is the repository Markdown file itself.

## Expected Outcome

- `BVF_SPEC.md` gains a tiny non-behavioral smoke-ticket marker.
- A lightweight automated test guards the presence and placement of that marker.
- No implementation, runtime behavior, or spec requirements change.
