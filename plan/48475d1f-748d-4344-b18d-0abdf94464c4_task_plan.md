# Task Plan: 48475d1f-748d-4344-b18d-0abdf94464c4

## Objective

Add a new isolated smoke marker document at `docs/terarchitect-smoke/ticket-06.md` with the exact required content, without changing code behavior.

## Constraints and Observations

- The ticket is intentionally docs-only and independent of the existing runtime and test flows.
- The repo already contains prior smoke markers as inert comments or doc markers in existing files, with some covered by narrow pytest assertions.
- There is no existing `docs/terarchitect-smoke/` directory, so the change will introduce a new isolated path.
- There is no app service, `docker-compose.yml`, frontend, or existing UI/E2E harness involved in this ticket, so dynamic localhost ports, compose project naming, and browser automation do not apply here.
- Because the requirement is exact text in a standalone file, the most appropriate TDD coverage is a focused pytest doc test that reads the file and asserts the required lines.

## Files Expected to Change

- `tests/test_terarchitect_smoke_ticket_06.py`
  Purpose: new failing test that defines the required artifact and exact content contract.
- `docs/terarchitect-smoke/ticket-06.md`
  Purpose: new isolated smoke marker document that satisfies the ticket.

## Order of Work

### 1. Add a focused failing test first

- Create `tests/test_terarchitect_smoke_ticket_06.py`.
- Add one narrow pytest test that:
  - resolves the repo root with `Path(__file__).resolve().parents[1]`
  - targets `docs/terarchitect-smoke/ticket-06.md`
  - fails if the file does not exist
  - reads the file as UTF-8
  - asserts the file contains:
    - top-level heading exactly `# Terarchitect Smoke Ticket 06`
    - line exactly `Ticket: 06`
    - line exactly `Purpose: verify competing attempts, winner selection, acceptance, and Ship Room composition on an isolated file.`
    - a short independence note stating the file is intentionally independent of the other smoke tickets
- Keep the assertions precise and local to this artifact. Do not broaden scope into README, packaging, or runtime behavior.

Dependency:
- None. This is the first red step.

### 2. Run the new targeted test and confirm it fails

- Run a narrow pytest command for the new file only, for example:
  - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_terarchitect_smoke_ticket_06.py`
- Expected initial failure:
  - missing file at `docs/terarchitect-smoke/ticket-06.md`
  - or missing required content if the file exists unexpectedly

Dependency:
- Step 1 must be complete.

### 3. Implement the minimum docs change to make the test pass

- Create the directory `docs/terarchitect-smoke/` if needed.
- Create `docs/terarchitect-smoke/ticket-06.md`.
- Add only the required content plus a short independence note.
- Keep the file minimal:
  - no frontmatter
  - no extra headings
  - no links
  - no references to runtime code or other smoke tickets beyond the independence note

Illustrative shape only:

```md
# Terarchitect Smoke Ticket 06

Ticket: 06
Purpose: verify competing attempts, winner selection, acceptance, and Ship Room composition on an isolated file.

This file is intentionally independent ...
```

Dependency:
- Step 2 should have established the failing baseline first.

### 4. Re-run the targeted test and get green

- Re-run:
  - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_terarchitect_smoke_ticket_06.py`
- Confirm the new test passes with no unrelated changes required.

Dependency:
- Step 3 must be complete.

### 5. Run a small regression slice for documentation-related tests

- Run a narrow existing docs-related subset to ensure the new isolated doc file does not interfere with current documentation checks:
  - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_readme_docs.py tests/test_bvf_spec_doc.py tests/test_csharp_plugin_readme.py tests/test_terarchitect_smoke_ticket_06.py`
- This is still lightweight and appropriate for a docs-only ticket.

Dependency:
- Step 4 must be green.

### 6. Refactor only if needed

- Refactor only if the new test is unnecessarily brittle or repetitive.
- Acceptable refactor scope:
  - simplify assertion structure in `tests/test_terarchitect_smoke_ticket_06.py`
  - tighten the wording of the independence note in `docs/terarchitect-smoke/ticket-06.md` without violating the exact required lines
- Do not refactor unrelated tests or documentation.

Dependency:
- Step 5 should be complete first so refactoring starts from a green state.

### 7. Final verification and review checklist

- Verify `git diff --stat` reflects only the planned test and doc additions.
- Verify no code behavior files changed.
- Verify the new document remains isolated under `docs/terarchitect-smoke/`.
- Verify the exact required strings are present and spelled exactly as the ticket specifies.

## Test Plan

### Unit-style / focused tests to add

- `tests/test_terarchitect_smoke_ticket_06.py`
  - one focused test for file existence and exact required content

### Existing tests to run as regression coverage

- `tests/test_readme_docs.py`
- `tests/test_bvf_spec_doc.py`
- `tests/test_csharp_plugin_readme.py`
- `tests/test_terarchitect_smoke_ticket_06.py`

### Integration tests

- No new integration test is required for this ticket.
- Rationale:
  - no service startup
  - no HTTP interface
  - no DB
  - no fixture-dependent workflow
  - no docker-compose in the repo for this change
- Therefore:
  - no test data seeding is needed beyond the new Markdown file itself
  - no dynamic port allocation is needed
  - no `docker compose up -d` / `down` lifecycle applies

### UI / E2E tests

- Not applicable.
- The ticket does not touch a frontend, browser workflow, or user-facing interactive surface.

## Dependencies Between Steps

1. Add the failing test before creating the doc file.
2. Confirm the test fails before implementation.
3. Add the minimal doc artifact to satisfy the test.
4. Re-run the targeted test to confirm green.
5. Run a narrow regression subset of existing documentation tests.
6. Refactor only if needed, then re-run the same test subset if any refactor occurs.

## Expected Outcome

- A single isolated Markdown marker file exists at `docs/terarchitect-smoke/ticket-06.md`.
- A focused pytest test locks the requirement so future changes cannot silently alter or remove the marker.
- No runtime code, packaging behavior, or service behavior changes.
