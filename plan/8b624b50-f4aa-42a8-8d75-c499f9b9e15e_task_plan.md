# Task Plan: Smoke comment marker in `tests/test_readme_docs.py`

## Goal
Add a single comment-only smoke marker near the top of `tests/test_readme_docs.py` and keep behavior unchanged.

## Files to touch
- `tests/test_readme_docs_smoke_marker.py` for the temporary regression test
- `tests/test_readme_docs.py` for the one-line comment insertion

## Execution Order

1. Add a failing regression test first.
   - Create `tests/test_readme_docs_smoke_marker.py`.
   - Read `tests/test_readme_docs.py` and assert that `# smoke comment marker` appears near the file header.
   - Keep the assertion focused on the marker location, not on unrelated formatting.
   - Use `splitlines()` and a small line-index check so the test proves the marker is in the intended spot.

2. Run the new regression test before making the code change.
   - Command: `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_readme_docs_smoke_marker.py -q`
   - Expected result: failure, because the marker is not yet present.

3. Implement the minimum change to satisfy the test.
   - Edit `tests/test_readme_docs.py`.
   - Add exactly one standalone comment line near the file header, matching the repo convention:
     - `# smoke comment marker`
   - Keep the existing test logic, imports, and assertions unchanged.
   - Do not add any runtime behavior, helper functions, or refactors.

4. Re-run the regression test and the existing README content test.
   - Command: `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_readme_docs_smoke_marker.py tests/test_readme_docs.py -q`
   - Confirm the new marker test passes and the original README assertion test still passes unchanged.

5. Do a final focused verification pass for nearby smoke-marker conventions if needed.
   - Only if the first verification exposes an unexpected formatting issue, run a narrow adjacent smoke subset such as `tests/test_pipeline_smoke_marker.py`.
   - No integration or UI/E2E tests are required because this ticket is comment-only and does not change runtime behavior.

## Dependencies
- Step 2 depends on Step 1.
- Step 3 depends on Step 2 failing as expected.
- Step 4 depends on Step 3.
- Step 5 is optional and only for unexpected validation issues.

## Test Strategy
- Unit/regression test added: `tests/test_readme_docs_smoke_marker.py`
- Existing unit test exercised unchanged: `tests/test_readme_docs.py`
- Integration tests: none
- Test data: none required

## Risk Control
- Keep the change to a single comment line so behavior cannot drift.
- Avoid exact formatting assumptions beyond the marker being near the top of the file, unless the repo convention requires a specific line index.
