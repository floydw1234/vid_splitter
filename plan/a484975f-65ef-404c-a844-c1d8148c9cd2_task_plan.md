# Task Plan: Smoke 06 - add BVF muxer comment

## Goal

Add exactly one tiny clarifying comment near the top of `vid_splitter/bvf_muxer.py`
explaining that BVF muxing writes container metadata without altering analysis
semantics, while preserving runtime behavior.

## Constraints and Observations

- The ticket is comment-only; no logic changes are expected.
- The repository already uses TDD-style source-text tests for smoke/comment tickets:
  - `tests/test_vid_splitter_package.py`
  - `tests/test_analyzer_package.py`
  - `tests/test_bvf_spec_doc.py`
- `vid_splitter/bvf_muxer.py` already describes BVF as packaging metadata plus
  media assets near the top-level module docstring and `BvfMuxer` class docstring.
- There is no `docker-compose.yml`/`compose.yml` in the repo, and no frontend/UI
  automation setup such as Playwright or Cypress. No service startup or dynamic
  port planning is needed for this ticket.

## Files Expected To Change

- `tests/test_bvf_muxer.py`
- `vid_splitter/bvf_muxer.py`

## TDD Execution Plan

### 1. Add a failing regression test first

- Update `tests/test_bvf_muxer.py` with a new test that reads
  `vid_splitter/bvf_muxer.py` as text and checks for a brief clarifying comment
  near the top of the file.
- Keep the assertion focused on the semantic boundary required by the ticket, not
  an overly rigid full-string match. A snippet-level assertion should look for
  terms such as `container metadata` and `analysis semantics`.
- Scope the check to the header portion of the file, for example by inspecting the
  first ~20-40 lines, so the test enforces “near the top” rather than allowing the
  comment to drift lower in the module.
- Follow the existing repo style for comment-only tests: use `Path(...).read_text()`
  and assert on lowercase text where practical.

Dependency:
- This step should be completed before editing `vid_splitter/bvf_muxer.py`.

### 2. Run the targeted test and confirm it fails

- Execute the targeted pytest selection for the new test in `tests/test_bvf_muxer.py`.
- Confirm the failure is specifically due to the missing clarifying comment, not an
  unrelated import or environment issue.

Dependency:
- Requires Step 1 to be committed locally in the worktree first.

### 3. Implement the minimum code change to pass

- Add exactly one tiny comment near the top of `vid_splitter/bvf_muxer.py`.
- Place it in a location that is visible immediately when opening the file, likely
  just below the module docstring or just above the `BvfMuxer` class if that still
  satisfies the “near the top” requirement.
- Use wording consistent with existing project terminology:
  - `BVF muxing`
  - `container metadata`
  - `analysis semantics`
- Do not alter imports, docstrings, constants, function bodies, or formatting beyond
  what is needed for the single comment addition.

Dependency:
- Should only begin after the failing test from Step 2 confirms the gap.

### 4. Re-run the targeted test to verify the fix

- Re-run the new test in `tests/test_bvf_muxer.py`.
- Verify it passes with no runtime behavior changes required.

Dependency:
- Requires Step 3.

### 5. Run the relevant local regression subset

- Run the full `tests/test_bvf_muxer.py` module to ensure the comment-only change
  did not accidentally disturb file layout assumptions or imports.
- Optionally run the existing smoke/comment tests as a lightweight regression set:
  - `tests/test_vid_splitter_package.py`
  - `tests/test_analyzer_package.py`
  - `tests/test_bvf_spec_doc.py`

Dependency:
- Requires Step 4 to have passed.

### 6. Refactor only if necessary

- Refactoring is unlikely for this ticket because the intended implementation is a
  single comment.
- If the new test proves too brittle during verification, refine the assertion to
  keep it intent-based while still checking:
  - the comment exists,
  - it is near the top of the file,
  - it describes metadata-vs-semantics clearly.

Dependency:
- Only if verification in Steps 4-5 exposes fragility.

## Planned Test Changes

### Unit / source-level tests

- Update `tests/test_bvf_muxer.py`:
  - Add one new test for the header comment presence and placement.
  - Reuse the repository’s existing source-text smoke-test pattern rather than
    introducing AST parsing or runtime inspection.

### Integration tests

- No new integration test is planned.
- Reason:
  - The ticket explicitly prohibits runtime behavior changes.
  - The change is documentation/comment-only within a Python source file.
  - The repo has no compose-backed app/service flow relevant to this file.
  - Existing integration tests would not add signal for a one-line comment.

## Order of Work Summary

1. Add a failing source-text test in `tests/test_bvf_muxer.py`.
2. Run the targeted pytest selection and confirm failure.
3. Add the single clarifying comment in `vid_splitter/bvf_muxer.py`.
4. Re-run the targeted test and confirm it passes.
5. Run `tests/test_bvf_muxer.py` and optionally the other smoke/comment tests.
6. Adjust the test only if it is unnecessarily brittle.

## Risks and Mitigations

- Risk: The test could overfit to an exact sentence and become noisy.
  - Mitigation: Assert on key semantic phrases and top-of-file placement, not the
    entire literal comment.
- Risk: Adding the comment in the wrong location could satisfy the wording but miss
  the “near the top” requirement.
  - Mitigation: Limit the test to the file header region.
- Risk: Editing a docstring instead of adding a standalone comment could broaden the
  change beyond the ticket’s request.
  - Mitigation: Plan for one standalone comment only.
