# Task Plan: Smoke comment marker in `test_pipeline.py`

## Goal
Add a single comment-only smoke marker near the header of [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py) without changing runtime behavior.

## Constraints
- Keep the change minimal and non-functional.
- Follow TDD: failing test first, then the smallest code change, then verification.
- Do not refactor unrelated parts of the script.

## Files To Touch
- [`tests/test_pipeline_smoke_marker.py`](/tmp/terarchitect_worker_zclzr6g1/repo/tests/test_pipeline_smoke_marker.py) or another narrowly scoped test file if the repo already has a better home for file-content assertions.
- [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py)

## Step-by-step Plan

1. Add a failing test that describes the desired marker placement.
   - Create a new unit test that reads [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py) as text.
   - Assert the file still begins with its existing module docstring.
   - Assert a single smoke marker comment exists immediately after the docstring and before the imports.
   - Keep the assertion narrow and structural, for example checking that the relevant header region contains:
     - the closing `"""` for the docstring,
     - a smoke marker comment line,
     - `import sys` on the next import line.
   - This test should fail before the implementation change because the marker is not yet present in `test_pipeline.py`.

2. Implement the smallest possible code change to satisfy the test.
   - Insert one comment-only marker near the top of [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py), directly after the module docstring and before the imports.
   - Use the existing repo convention for smoke markers, matching the style already used in files like [`analyzer/skin_detector.py`](/tmp/terarchitect_worker_zclzr6g1/repo/analyzer/skin_detector.py).
   - Do not modify any executable statements, imports, or test logic.

3. Verify the change with targeted tests.
   - Run the new smoke-marker test only first to confirm it passes.
   - Run the existing adjacent test subset that exercises file-content assertions around the analyzer/test marker conventions, to ensure the new comment does not disturb nearby expectations.
   - If the repo has a standard fast test command for this area, prefer that over the full suite.

4. Refactor only if the test exposes a readability issue.
   - If the new test can reuse an existing helper or naming pattern used by the smoke-marker tests, adopt that pattern.
   - Avoid broad cleanup. The deliverable is a one-line comment marker and a matching verification test.

## Test Order
- First: failing test in `tests/test_pipeline_smoke_marker.py` (or equivalent).
- Second: implementation in `test_pipeline.py`.
- Third: targeted pytest run for the new test.
- Fourth: adjacent smoke/file-content tests if needed for confidence.

## Dependencies
- The test depends only on the current contents of [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py).
- No fixtures, sample data, external services, docker compose, UI automation, or dynamic ports are needed.
- No integration test data is required because this ticket is a pure source-comment change.

## Acceptance Check
- [`test_pipeline.py`](/tmp/terarchitect_worker_zclzr6g1/repo/test_pipeline.py) contains exactly one new smoke marker comment near the header.
- The file’s behavior remains unchanged.
- The new test passes and existing nearby tests continue to pass.
