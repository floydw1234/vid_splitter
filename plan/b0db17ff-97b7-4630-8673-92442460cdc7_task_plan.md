# Ticket Plan: Smoke 10 - add marlin analyze comment

## Goal

Add exactly one tiny clarifying comment near the top of `analyzer/marlin_analyze.py` explaining that MARLIN analysis is an optional model-backed path, without changing runtime behavior.

## Constraints and context

- The ticket is comment-only; no functional behavior should change.
- The repository already has a smoke-ticket testing pattern that validates comment markers by reading file contents directly.
- `analyzer/marlin_analyze.py` is already described in project docs as an alternative analyzer path, so the new comment should align with that terminology.
- No web service, Docker Compose stack, database, fixed port, or UI/E2E surface is involved in this ticket, so none of the dynamic-port or service-lifecycle guidance applies here.

## Files expected to change

- `tests/test_marlin_analyze.py` (new test file)
- `analyzer/marlin_analyze.py` (single tiny comment near the top)

## Order of work

1. Inspect the target module header and existing smoke-ticket tests.
2. Add a failing test that asserts the top of `analyzer/marlin_analyze.py` contains a clarifying comment about MARLIN being optional/model-backed.
3. Run the new narrow test to confirm it fails before implementation.
4. Add exactly one small comment near the top of `analyzer/marlin_analyze.py`.
5. Re-run the narrow test to confirm it passes.
6. Run nearby regression tests to confirm the comment-only change did not disturb existing expectations.
7. If needed, do a minimal refactor only for wording/placement clarity, with no behavioral edits.

## TDD execution details

### Step 1: Add the failing test first

Create `tests/test_marlin_analyze.py` with a focused file-content assertion modeled on the existing smoke tests such as:

- `tests/test_analyzer_package.py`
- `tests/test_vid_splitter_package.py`

Planned assertions:

- Read `analyzer/marlin_analyze.py` from disk using `Path`.
- Limit the check to the top section of the file so the test enforces "near the top" rather than allowing the comment anywhere.
- Assert that the top-of-file text contains wording indicating:
  - `marlin`
  - `optional`
  - `model-backed` or equivalent wording if repo style suggests a small variation

Implementation note:

- Prefer a robust substring check over exact whole-line matching unless the ticket requires fixed text.
- Keep the test independent of heavyweight imports so it does not trigger model loading.

Brief example shape:

```py
top_text = "\n".join(target.read_text(encoding="utf-8").splitlines()[:20]).lower()
assert "optional" in top_text
```

### Step 2: Run the new test and confirm failure

Run only the new test first, for example:

```bash
env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_marlin_analyze.py
```

Expected result:

- The test fails because the required clarifying comment does not yet exist.

Dependency:

- This must happen before editing `analyzer/marlin_analyze.py` to preserve TDD order.

### Step 3: Implement the minimum code change

Edit `analyzer/marlin_analyze.py` and add exactly one tiny clarifying comment near the top of the file, placed close to the module docstring/import area.

Planned scope:

- One comment only.
- No import changes.
- No logic changes.
- No docstring rewrite unless inspection shows the comment must sit immediately under the docstring for clarity; even then, keep the change comment-only.

Planned wording characteristics:

- Say MARLIN analysis is an optional model-backed path.
- Stay consistent with existing repo language such as "alternative analyzer path".
- Avoid changing runtime semantics or introducing broader architecture commentary.

### Step 4: Re-run the narrow test

Run:

```bash
env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_marlin_analyze.py
```

Expected result:

- The test passes.

### Step 5: Run targeted regression tests

Run the closest existing smoke/content tests that validate the same style of repository invariant:

- `tests/test_analyzer_package.py`
- `tests/test_vid_splitter_package.py`

Suggested command:

```bash
env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_marlin_analyze.py tests/test_analyzer_package.py tests/test_vid_splitter_package.py
```

Expected result:

- All pass, confirming the new smoke-ticket test fits the established pattern and the codebase remains unchanged behaviorally.

## Integration test plan

No integration test is needed for this ticket because:

- The requested change is documentation-only inside a Python source file.
- There is no service, endpoint, UI, Docker environment, or runtime path change to validate.
- Existing real-video integration tests for the Marlin path are unrelated to the requested smoke comment and would add noise rather than confidence for this ticket.

If broader regression confidence is desired after implementation, it should remain optional and out of scope for this ticket:

- `tests/test_real_video_integration.py` is heavyweight and depends on external tools/test assets.

## Dependencies between steps

- The new test file must be added before changing `analyzer/marlin_analyze.py`.
- The initial pytest run must happen before implementation to establish a failing state.
- The comment edit depends on the final expected test wording.
- Targeted regression tests happen only after the narrow test passes.

## Risks and mitigations

- Risk: The test could be too brittle if it matches an exact sentence.
  - Mitigation: Assert on a few required terms in the top-of-file text rather than one exact line.
- Risk: The comment could accidentally duplicate or conflict with the module docstring.
  - Mitigation: Keep the wording short and complementary, not a restatement of the full docstring.
- Risk: Importing the module in a test could load heavy dependencies.
  - Mitigation: Use file reads only; do not import `analyzer.marlin_analyze` in the new test.

## Definition of done

- `tests/test_marlin_analyze.py` exists and fails before the code change.
- `analyzer/marlin_analyze.py` contains exactly one small clarifying comment near the top indicating the MARLIN path is optional/model-backed.
- The new test passes.
- Targeted related pytest checks pass.
- No runtime behavior or public interface changes are introduced.
