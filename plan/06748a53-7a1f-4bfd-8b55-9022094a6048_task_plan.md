# Task Plan: 06748a53-7a1f-4bfd-8b55-9022094a6048

## Objective

Harden `LLMTopicClassifier._parse_topics` in [analyzer/topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/analyzer/topic_classifier.py) so topic parsing succeeds when an LLM wraps the JSON array in markdown code fences or explanatory text, while continuing to reject unknown topics. Add focused unit tests in [tests/test_topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/tests/test_topic_classifier.py) that call `_parse_topics` directly and do not make live HTTP requests.

## Scope And Constraints

- Touch only the parser and its focused Python tests unless review during implementation shows a strictly necessary adjacent change.
- Use TDD for each parser behavior: add or adjust a failing test first, then implement the smallest parser change to pass, then refactor if needed.
- Avoid live HTTP calls entirely in new coverage.
- Prefer custom in-test topic taxonomies to keep expected results explicit and isolated from the default taxonomy.
- No Docker Compose or app service orchestration is needed for this ticket because the change is a pure parser/unit-test task and the repo does not currently expose a compose setup for this workflow.

## Files Expected To Change

- [tests/test_topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/tests/test_topic_classifier.py)
- [analyzer/topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/analyzer/topic_classifier.py)

## Execution Order

1. Establish a reliable local test command for the targeted file.
2. Add or refine parser unit tests in `tests/test_topic_classifier.py` so the desired cases fail first.
3. Update `_parse_topics` in `analyzer/topic_classifier.py` with the minimum parsing logic needed to satisfy the new failures.
4. Refactor only if the parser becomes repetitive or ambiguous after the fix.
5. Re-run the focused topic-classifier tests.
6. Optionally run a broader nearby Python test slice if the environment supports it.

## Detailed TDD Steps

### 1. Baseline Test Harness

- Confirm the narrowest runnable command for this repo’s Python tests.
- Expected command shape:

```bash
PYTHONPATH=. env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_topic_classifier.py
```

- If imports or dependencies fail before reaching the parser tests, note that as an environment issue, not a reason to widen the ticket.
- Dependency note:
  The existing test file imports `MovieAnalyzer`, which may pull in broader analyzer dependencies. If that import path blocks the focused parser tests, the first test change should reduce unnecessary import coupling while preserving existing behavior coverage.

### 2. Write Or Update Failing Tests First

Update [tests/test_topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/tests/test_topic_classifier.py) in this order:

1. Keep or add a small helper like `make_classifier(topics=...)` that constructs `LLMTopicClassifier` without live client setup.
2. Add a direct `_parse_topics` test for a plain JSON array baseline.
   Example snippet:

```python
parsed = classifier._parse_topics('["politics", "sports"]')
```

3. Add a failing test for fenced JSON with a `json` language tag.
4. Add a failing test for fenced JSON wrapped in explanatory prose before and after the fence.
5. Add a failing test for an inline array embedded in explanatory text such as `Topics present: ["sports"]`.
6. Add a failing test proving unknown labels are filtered out after extraction.
7. Add a failing test for non-JSON explanatory text returning `[]`.
8. Add a failing precedence test showing a valid fenced JSON block should win over unrelated bracketed text elsewhere in the response.

Dependencies between tests:

- Steps 3 and 4 depend on the parser being able to inspect fenced blocks separately from the raw response string.
- Step 8 depends on the parser search order being explicit and stable.

### 3. Implement Minimum Parser Changes

Modify [analyzer/topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/analyzer/topic_classifier.py) only after the new/updated tests fail.

Planned implementation sequence:

1. Preserve the current “try full string as JSON first” behavior.
2. Add or refine a fenced-code-block extraction pass using a regex that tolerates:
   - optional `json` language tag
   - leading/trailing whitespace inside the fence
   - explanatory text outside the fence
3. Add or refine a bracketed-array extraction pass for inline responses that contain a JSON array inside surrounding prose.
4. Reuse a single validation helper such as `_parse_topic_list` so all extraction paths still enforce:
   - parsed value must be a JSON list
   - returned topics must exist in `self.topics`
5. Keep fallback behavior conservative. If no valid JSON array is found, return only validated matches from the fallback path, or `[]` if nothing is clearly present.

Implementation guardrails:

- Do not broaden parsing to infer arbitrary free-form labels.
- Do not add network behavior or alter `classify_segment` request structure for this ticket.
- Keep the parser deterministic and side-effect free.

### 4. Refactor Only If Needed

After the parser passes the new tests, evaluate whether the code should be tightened without changing behavior:

- Collapse repeated “extract then validate” logic into a helper if the final implementation duplicates the same pattern multiple times.
- Keep regexes readable and localized to `_parse_topics`.
- Preserve the caller contract: `_parse_topics` returns `List[str]`.

This step depends on all targeted tests passing first.

### 5. Verify Focused Coverage

Run the targeted test file again:

```bash
PYTHONPATH=. env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_topic_classifier.py
```

Success criteria:

- New parser tests pass.
- Existing topic-classifier tests still pass.
- No live HTTP calls occur during the test run.

### 6. Optional Adjacent Verification

If the environment has the required Python dependencies installed, run one adjacent test slice to check for unintended regressions near analyzer behavior. Keep this narrow; do not expand into heavy integration suites unless they are already fast and local.

Candidate command:

```bash
PYTHONPATH=. env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_topic_classifier.py tests/test_integration.py -k topic
```

If the environment is missing dependencies such as `numpy`, record that limitation and stop after the focused test file.

## Test Plan Summary

### Unit Tests To Add Or Update

- [tests/test_topic_classifier.py](/home/william/Documents/codingProj/vid_splitter/tests/test_topic_classifier.py)
- Cases to cover:
  - plain JSON array
  - fenced JSON array
  - fenced JSON with explanatory wrapper text
  - inline JSON array in prose
  - unknown-topic filtering
  - empty result for non-JSON non-matching text
  - precedence of fenced JSON over unrelated bracketed text

### Integration Tests

- No new integration test is required for this ticket because the behavior is isolated to a pure parsing helper and the ticket explicitly asks for direct `_parse_topics` coverage without live HTTP calls.
- No test data, fixtures, sample media, database state, or dynamic localhost ports are needed.

## Risks And Mitigations

- Risk: the test module imports analyzer code that pulls in optional heavy dependencies.
  Mitigation: keep parser tests direct and, if necessary, reduce import coupling in the test file so `_parse_topics` coverage can run in a lightweight environment.

- Risk: a loose regex could capture non-JSON bracketed text too aggressively.
  Mitigation: preserve the validation step with `json.loads(...)` and prioritize fenced JSON before inline bracket scans.

- Risk: fallback substring matching could accidentally return topics from explanatory prose.
  Mitigation: keep JSON extraction paths first and validate any fallback results strictly against the custom taxonomy used in tests.

## Definition Of Done

- `tests/test_topic_classifier.py` contains focused direct `_parse_topics` coverage for fenced and explanatory-text-wrapped JSON responses.
- `_parse_topics` correctly extracts and validates topic arrays from the tested response shapes.
- Focused pytest execution passes without requiring network access.
