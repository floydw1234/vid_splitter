## Ticket

- ID: `e8023783-1d24-4c71-a53a-9ebfa39820fd`
- Title: `Smoke 06: clarify BVF probe tool comment`
- Scope: make one tiny comment-only change in `tools/bvf_probe.py` identifying the file as concurrency smoke ticket 06, with no runtime behavior change.

## Constraints and repo context

- Primary code target: `tools/bvf_probe.py`
- Existing BVF probe tests: `tests/test_bvf_probe.py`
- The repo uses `pytest` for Python tests.
- There is no `docker-compose.yml` / `compose.yaml` in this repo, so no containerized integration-test plan applies here.
- There is no frontend/UI test surface involved in this ticket.
- Because the ticket explicitly requires a comment-only change, avoid edits to:
  - CLI help text
  - return values / exit codes
  - runtime logic
  - module docstring if a plain `#` comment is sufficient

## Execution plan

### 1. Add the identifying comment to `tools/bvf_probe.py`

- Read `tools/bvf_probe.py` again immediately before editing.
- Add one brief non-executable comment near the top of the file that identifies this as concurrency smoke ticket 06.
- Prefer a plain `#` comment over changing user-facing text or executable code.
- Do not modify runtime logic, CLI help text, exit behavior, or imports unless formatting requires it.

Files to touch in this step:
- `tools/bvf_probe.py`

### 2. Verify the file content

- Re-open `tools/bvf_probe.py` after the edit.
- Confirm the new comment is present, correctly worded, and placed in a readable location.
- Confirm the production diff is comment-only and does not introduce unrelated edits or whitespace churn.

Verification focus:
- the identifying comment exists
- the file still reads cleanly
- no executable behavior was changed

### 3. Run existing tests to ensure no accidental regressions

- Run the existing BVF probe test module:
  - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_bvf_probe.py`
- Use that suite as the regression check because it already covers the probe tool’s current behavior.
- No new tests are planned for this ticket because the requested change is comment-only.

## Planned order of work

1. Add the single identifying comment to `tools/bvf_probe.py`.
2. Verify the updated file content and diff.
3. Run `tests/test_bvf_probe.py` as the regression check.

## Files expected to change

- `tools/bvf_probe.py`
  - add one short source comment identifying concurrency smoke ticket 06

## Tests to add or update

- No test files are expected to change.
- Re-run existing tests in `tests/test_bvf_probe.py` as the regression check for no behavior change.

## Integration / environment notes

- No Docker Compose plan applies because the repo does not include a compose file.
- No localhost port planning applies because this ticket does not start any network service.
- No UI/E2E automation applies because the ticket does not touch frontend code.
- No new fixtures or sample media are required; existing BVF probe tests already generate their own temporary test data.

## Completion criteria

- `tools/bvf_probe.py` contains a concise, non-executable comment identifying concurrency smoke ticket 06.
- Existing `tests/test_bvf_probe.py` coverage remains green, showing no runtime behavior regression.
- The production diff is comment-only.
