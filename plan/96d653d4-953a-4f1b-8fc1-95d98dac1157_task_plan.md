## Ticket

`96d653d4-953a-4f1b-8fc1-95d98dac1157` — Smoke 05: add skin detector comment

Goal: add exactly one tiny clarifying comment near the top of `analyzer/skin_detector.py` explaining that skin detection is heuristic and conservative, with no runtime behavior change.

## Constraints and assumptions

- Keep the change comment-only in production code.
- Preserve all current imports, docstrings, and executable behavior.
- Follow existing smoke-ticket testing style already present in `tests/test_analyzer_package.py` and `tests/test_vid_splitter_package.py`: assert file content directly instead of inventing a broader behavioral harness for a comment-only ticket.
- No integration environment, service startup, Docker, ports, or UI/E2E work is needed for this ticket because the requested change does not touch any service boundary or frontend path, and the repo does not currently provide a `docker-compose` workflow for this area.

## Files expected to change

- `tests/test_analyzer_package.py`
- `analyzer/skin_detector.py`

## Execution order (TDD)

1. Review the target file and nearby conventions.
   - Re-read `analyzer/skin_detector.py` to identify a safe placement near the top of the module.
   - Re-read `tests/test_analyzer_package.py` for the existing smoke-ticket assertion pattern.
   - Dependency: none.

2. Add a failing test first in `tests/test_analyzer_package.py`.
   - Create a new focused test that reads `analyzer/skin_detector.py` as text and asserts the presence of a short phrase capturing the required meaning, for example a snippet like `"heuristic and conservative"`.
   - Keep the test file-content based so it verifies the ticket directly and does not require importing OpenCV-heavy analyzer components.
   - Expected initial result: test fails because the comment does not exist yet.
   - Dependency: step 1.

3. Run the targeted test to confirm it fails for the right reason.
   - Run only the narrow test module, e.g. `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_analyzer_package.py`.
   - Confirm the failure is specifically the missing text assertion, not an unrelated environment issue.
   - Dependency: step 2.

4. Implement the minimum production change in `analyzer/skin_detector.py`.
   - Add exactly one tiny clarifying comment near the top of the file.
   - Place it high enough to satisfy the ticket, but keep it outside runtime logic so behavior cannot change.
   - Keep wording aligned with the code’s actual behavior: the detector uses HSV thresholds and requires both skin coverage and contiguous-region thresholds before flagging.
   - Avoid adding extra comments, changing docstrings, or reflowing unrelated lines.
   - Dependency: step 3.

5. Re-run the targeted test and confirm it passes.
   - Re-run `tests/test_analyzer_package.py`.
   - Verify the new assertion passes and the existing analyzer package smoke test still passes.
   - Dependency: step 4.

6. Run a lightweight regression check for neighboring smoke tests.
   - Run the small related smoke-style tests that assert comment markers in nearby modules:
     - `tests/test_analyzer_package.py`
     - `tests/test_vid_splitter_package.py`
   - Purpose: confirm the new test pattern remains consistent and no unrelated package metadata assertions regress.
   - Dependency: step 5.

7. Final review and refactor check.
   - Confirm no refactor is needed because the change should remain minimal and comment-only.
   - Verify `git diff` shows only the expected test addition and one small comment in `analyzer/skin_detector.py`.
   - Dependency: step 6.

## Planned test changes

### Unit tests to add/update

- Update `tests/test_analyzer_package.py`
  - Add one new test dedicated to `analyzer/skin_detector.py`.
  - Test shape:
    - Resolve repo-relative path to `analyzer/skin_detector.py`
    - Read file text
    - Assert presence of the required brief wording
  - Reasoning:
    - This is the most direct TDD proof for a comment-only requirement.
    - It avoids importing `cv2` or executing analyzer code for a non-behavioral ticket.

### Integration tests

- None planned.
  - Reason: the ticket explicitly forbids runtime behavior changes and only asks for a clarifying comment.
  - If repository policy required an integration check anyway, the smallest safe fallback would still be a targeted pytest invocation against existing tests, not a new service-level harness.

## Dependencies between steps

- The production edit depends on first landing the failing file-content test.
- Verification depends on targeted pytest passing after the comment is added.
- Final review depends on confirming the diff stays minimal and comment-only.

## Risks to watch

- Avoid choosing assertion text that is too brittle if the final wording is slightly different; prefer a short required phrase rather than a full-line exact match.
- Avoid placing the comment inside a docstring if that would alter documentation semantics more than necessary.
- Avoid importing `analyzer.skin_detector` in the test, since environment-specific OpenCV availability could create noise unrelated to the ticket.

## Expected deliverable

- One new smoke-style test in `tests/test_analyzer_package.py`.
- One tiny clarifying comment near the top of `analyzer/skin_detector.py`.
- Targeted pytest evidence showing the new test failed before the edit and passed after the edit.
