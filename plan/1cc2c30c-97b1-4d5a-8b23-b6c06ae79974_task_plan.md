# Task Plan: Smoke 07: add package init comment

## Goal

Add exactly one tiny clarifying comment near the top of `vid_splitter/__init__.py` explaining that the package exposes BVF/video splitting utilities, while preserving runtime behavior.

## Constraints And Existing Signals

- `tests/test_vid_splitter_package.py` currently asserts `vid_splitter.__doc__ is None`, so the plan must avoid adding a package docstring.
- The ticket explicitly calls for a comment-only change and no runtime behavior change.
- The file `vid_splitter/__init__.py` currently contains only a top-level comment, so the implementation should remain minimal and inert.

## Files Expected To Change

- `tests/test_vid_splitter_package.py`
- `vid_splitter/__init__.py`

## Order Of Work

1. Review the current package test and package init file together.
   - Confirm the existing assertions around `__doc__` and the current smoke-marker comment.
   - Confirm there are no other tests depending on exact file contents beyond the smoke marker phrase.

2. Write the failing test change first in `tests/test_vid_splitter_package.py`.
   - Keep the existing `__doc__ is None` assertion intact to protect runtime/package metadata behavior.
   - Update or replace the current comment-content assertion so it expects the new clarifying language.
   - Prefer a focused substring assertion against the file text rather than an exact full-file snapshot.
   - Example target shape: assert the file contains terms such as `"BVF"` and `"video splitting utilities"` while still preserving the required smoke marker if that marker must remain present.

3. Run the targeted unit test and confirm it fails for the expected reason.
   - Command scope: run only `tests/test_vid_splitter_package.py`.
   - Purpose: verify the test is actually driving the comment update instead of passing against the old comment.

4. Implement the minimum code change in `vid_splitter/__init__.py`.
   - Replace the existing top comment with exactly one tiny clarifying comment near the top of the file.
   - Keep the file free of executable code, imports, or docstrings.
   - Preserve any required smoke-ticket marker wording if the updated test or project convention still requires it.

5. Re-run the targeted unit test and confirm it passes.
   - Validate both expectations:
     - `vid_splitter.__doc__` remains `None`
     - the new clarifying comment text is present in the file

6. Refactor only if needed.
   - If the test can be made clearer without broadening scope, simplify naming/assertions in `tests/test_vid_splitter_package.py`.
   - Do not add new abstractions or helper functions for this ticket unless they remove duplication already present.

7. Run a lightweight regression check after the minimal fix.
   - Re-run `tests/test_vid_splitter_package.py`.
   - Optionally run the adjacent package metadata test `tests/test_analyzer_package.py` if a quick sanity comparison is useful, but this is secondary because the ticket is isolated to `vid_splitter`.

## Test Plan

### Unit Tests

- Update `tests/test_vid_splitter_package.py` first.
  - Keep: package docstring remains absent.
  - Update: file-text assertion to verify the new clarifying package comment.

### Integration Tests

- No new integration test is planned.
  - Reason: this ticket is a comment-only package metadata/file-content change with no service, CLI, or cross-module runtime behavior impact.
  - No test data, fixtures, ports, web services, or Docker Compose setup are required for appropriate coverage.

## Dependencies Between Steps

- Step 2 depends on Step 1 to confirm the current assertions and avoid accidentally planning a docstring.
- Step 4 depends on Step 3 so the implementation is validated by a demonstrably failing test first.
- Step 5 depends on Step 4.
- Step 6 is conditional on whether the initial minimal change leaves any obvious test cleanup.

## Definition Of Done

- `tests/test_vid_splitter_package.py` expresses the desired clarifying comment behavior and still protects against introducing a package docstring.
- `vid_splitter/__init__.py` contains exactly one tiny clarifying top-level comment describing the package as exposing BVF/video splitting utilities.
- The targeted unit test passes after the implementation change.
