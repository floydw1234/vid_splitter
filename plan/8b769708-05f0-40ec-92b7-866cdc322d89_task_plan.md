# Task Plan: 8b769708-05f0-40ec-92b7-866cdc322d89

## Ticket Summary

Make one tiny comment-only change in `tools/bvf_player.py` that identifies the file as concurrency smoke ticket 07, without changing runtime behavior.

## Constraints and Planning Notes

- The ticket explicitly forbids runtime behavior changes.
- The requested change is comment-only, so the implementation must avoid edits to executable code, CLI output, docstring-observable runtime values, tests, fixtures, or generated artifacts.
- Normal TDD is not a clean fit for a pure comment marker. Adding a test that asserts comment text would create a brittle, low-value source-coupled test without protecting behavior. The plan therefore uses a TDD-oriented validation flow:
  - establish a behavioral baseline with existing targeted tests first,
  - make the smallest possible non-functional edit,
  - rerun the same tests to prove no regression,
  - avoid adding new tests unless the implementation scope expands beyond a comment-only change.

## Files Expected To Change

- `tools/bvf_player.py`
- `plan/8b769708-05f0-40ec-92b7-866cdc322d89_task_plan.md`

## Files Expected To Be Inspected Only

- `tools/test_bvf_player.py`
- `README.md`

## Order of Work

1. Confirm the precise insertion point in `tools/bvf_player.py`.
   - Re-read the file header and nearby section banners.
   - Prefer a normal `# ...` block comment near an existing section header, not a module docstring edit.
   - Reason: editing the top docstring changes `__doc__`, which is technically observable at runtime.

2. Establish the pre-change behavioral baseline using existing tests.
   - Run targeted BVF player tests before editing:
     - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tools/test_bvf_player.py`
   - If the suite is too slow or environment-limited, fall back to the most relevant JSON/list/dry-run tests in `tools/test_bvf_player.py`.
   - Record that these tests pass before the comment edit.

3. TDD decision point: do not add a new failing test for comment text unless scope changes.
   - For this ticket as written, do not create a test like “file contains smoke 07 comment”.
   - Reason: that would test source wording rather than behavior, and it would increase maintenance cost with no runtime safety benefit.
   - If, during implementation, the requirement changes from “comment-only marker” to “visible documentation contract” or “CLI/help text clarification,” then add a failing test first for that externally observable behavior before any code change.

4. Apply the minimum implementation in `tools/bvf_player.py`.
   - Insert one concise block comment such as:
     - `# Concurrency smoke ticket 07: comment-only marker; no runtime behavior change.`
   - Keep the comment adjacent to the relevant file/section context.
   - Do not alter spacing, imports, strings, logic, parser options, output formatting, or surrounding comments unless required for readability.

5. Refactor pass.
   - Verify no refactor is needed beyond preserving local readability.
   - If the inserted comment looks out of place, adjust only its location or wording while keeping it comment-only.
   - Do not widen scope into doc cleanup elsewhere.

6. Post-change regression verification.
   - Rerun the same targeted tests used for the baseline:
     - `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tools/test_bvf_player.py`
   - Optionally run one focused CLI smoke command if a fixture is readily available from existing tests, but only if it does not require adding data or altering the repo.

7. Final review before closeout.
   - Inspect the diff to confirm only the intended comment was added in `tools/bvf_player.py` plus this plan file.
   - Confirm there are no changes to runtime output, serialized JSON, help text, or docs consumed by tests.

## Test Plan

### Unit Tests

- Reuse existing tests in `tools/test_bvf_player.py`.
- Primary verification target:
  - player parsing,
  - playback sequence resolution,
  - `--dry-run --json`,
  - `--list --json`,
  - argument validation for `--json`.

### Integration Tests

- No new integration test is planned.
- Reason: the ticket does not alter runtime behavior, data flow, CLI contract, or service boundaries.
- No new fixtures or sample BVF data are needed because existing test fixtures already cover the BVF player behavior that must remain unchanged.

### Frontend / UI / E2E

- None.
- The repo does not expose frontend/UI work for this ticket, and the requested change does not touch a UI surface.

## Environment and Service Plan

- No `docker-compose` file is present in the repository root or nearby project structure based on initial inspection.
- No web service startup, dynamic localhost port allocation, or compose project isolation is needed for this ticket.
- If later discovery contradicts this and a service-backed test becomes necessary, use:
  - a unique compose project name,
  - dynamic host port mapping,
  - runtime injection of resolved host/port into tests via env/config,
  - `docker compose down` after test completion.

## Dependencies Between Steps

- Step 2 depends on confirming the target file and current tests.
- Step 4 depends on the Step 3 decision that no new brittle source-text test should be introduced.
- Step 6 depends on Step 4 completing with a comment-only diff.
- Step 7 depends on successful regression verification in Step 6.

## Expected Outcome

- `tools/bvf_player.py` contains a small, explicit comment identifying concurrency smoke ticket 07.
- Existing BVF player tests pass unchanged before and after the edit.
- No runtime behavior, CLI behavior, serialized JSON output, or fixtures are modified.
